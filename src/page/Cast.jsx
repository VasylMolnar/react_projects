import React from 'react';
import MovieDetailsPage from './MovieDetailsPage';

const Cast = () => {
  return (
    <div>
      Cast (ActorsList) we have no photos or additional information about them.
      We create a actors name in MovieDetailsPage.jsx
    </div>
  );
};

export default Cast;
